import { showInConstruction } from "../../helpers/animations/showInConstructionMessage";

export const part2 = (ctx: CanvasRenderingContext2D) => {
  showInConstruction(ctx, "", "Mission accomplished");
};
